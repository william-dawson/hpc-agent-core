Record one workflow per scheduler with `mode="record"` only while cell2026 is
reachable, then replay the completed cache offline. Preserve the queue/host
selector, scheduler, resources, parallel environment, container choice,
rendered script, registry-routed status, and output. For Slurm, download or
save output before its scheduler record expires. State that the original code
was live-tested in 2026 but this unified port needs a fresh recording.
