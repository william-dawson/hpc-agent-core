Record the first real TSUBAME workflow with `mode="record"` only when the
facility is reachable, then replay the completed cache offline. Include the
chosen resource type and count, group or explicit trial choice, priority,
modules, launcher, rendered Grid Engine script, job status, and output. State
that the unified-hub port was built without live access until a fresh recording
proves otherwise.
