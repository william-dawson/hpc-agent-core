Record the first real Irene workflow with `mode="record"` only when TGCC is
reachable, then replay the completed cache offline. Preserve the chosen Bridge
partition, project, filesystem declaration, tasks/cores, modules, rendered
`#MSUB` script, job state, accounting, and output. State that the unified port
was built without live access until the recording proves the entire workflow.
